version: "3.8"

services:
  backend:
    build: 
      context: ./services/backend
    image: eqi30-backend:latest
    container_name: eqi30_backend
    restart: always
    ports:
      - "8000:8000"
    env_file:
      - ./services/backend/.env
    # Optional: If you want to use PostgreSQL instead of SQLite, uncomment the below depends_on section and the db service
    # depends_on:
    #   - db
    
  # db:
  #   image: postgres:15-alpine
  #   container_name: eqi30_db
  #   restart: always
  #   environment:
  #     POSTGRES_USER: eqi30_user
  #     POSTGRES_PASSWORD: your_strong_password
  #     POSTGRES_DB: eqi30_db
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data

# volumes:
#   postgres_data:
